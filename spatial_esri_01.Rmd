# Mapping Locations from Unstructured Text

lapply(c("tidyverse","rvest"))

XML, HTML, Reports, text or Media are unstructured text sources.

## Goals

| Transform unstructured text in a document to locations on a map

